name: Notify Telegram

on:
  workflow_dispatch: {}
  issues:
    types: [opened, reopened, closed, assigned, labeled, unlabeled]
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, reopened, closed, ready_for_review, review_requested, converted_to_draft]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]
  discussion:
    types: [created, edited, answered, pinned, unpinned]
  discussion_comment:
    types: [created]
  release:
    types: [published]
  workflow_run:
    workflows: ["Test project creation"] # Названия workflows, за которыми следим
    types: [completed]

permissions:
  contents: read

jobs:
  notify:
    if: github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Сформировать текст сообщения
        id: build
        uses: actions/github-script@v7
        with:
          script: |
            const event = context.eventName;
            const p = context.payload;
            const {owner, repo} = context.repo;

            // Экранирование для MarkdownV2
            const esc = (s = '') =>
              String(s).replace(/([_*\[\]()~`>#+\-=|{}.!\\])/g, '\\$1');

            const trim = (s = '', max = 3500) =>
              (s.length > max) ? (s.slice(0, max - 1) + '…') : s;

            const actor = p.sender?.login || context.actor || 'unknown';
            let title = '', url = '', body = '';
            const footer = `Repo: ${owner}/${repo}\nby ${actor}`;
            let skip = false;

            if (event === 'workflow_dispatch') {
              title = '🧪 Тестовое уведомление';
              url = `https://github.com/${owner}/${repo}/actions`;
              body = 'Проверка доставки сообщений в Telegram.';
            } else if (event === 'workflow_run') {
              const wr = p.workflow_run;
              if (!wr || wr.conclusion !== 'failure') {
                skip = true;
                core.setOutput('skip', 'true');
                return; // выходим без сборки сообщения
              }
              const name = p.workflow?.name || wr.name || 'Workflow';
              title = `❌ Упал workflow: ${name}`;
              url = wr.html_url;

              // Продолжительность
              let dur = '';
              try {
                const start = new Date(wr.run_started_at).getTime();
                const end   = new Date(wr.updated_at || wr.created_at).getTime();
                const sec = Math.max(0, Math.round((end - start)/1000));
                const m = Math.floor(sec / 60), s = sec % 60;
                dur = `${m}m ${s}s`;
              } catch (_) {}

              const head = [
                `Branch: ${wr.head_branch}`,
                `Conclusion: ${wr.conclusion}`,
                dur ? `Duration: ${dur}` : null,
                `Event: ${wr.event}`
              ].filter(Boolean).join('\n');

              const commitMsg = wr.head_commit?.message || '';
              body = [head, '', commitMsg].join('\n');
            } else {
              switch (event) {
                case 'issues':
                  title = `🆕 Issue ${p.action}: #${p.issue.number} ${p.issue.title}`;
                  url   = p.issue.html_url;
                  body  = p.issue.body || '';
                  break;
                case 'issue_comment':
                  title = `💬 Комментарий к issue #${p.issue.number} от ${p.comment.user.login}`;
                  url   = p.comment.html_url;
                  body  = p.comment.body || '';
                  break;
                case 'pull_request':
                  title = `🔀 PR ${p.action}: #${p.pull_request.number} ${p.pull_request.title}`;
                  url   = p.pull_request.html_url;
                  body  = p.pull_request.body || '';
                  break;
                case 'pull_request_review':
                  title = `📝 Review ${p.action} PR #${p.pull_request.number} от ${p.review.user.login}`;
                  url   = p.review.html_url || p.pull_request.html_url;
                  body  = p.review.body || '';
                  break;
                case 'pull_request_review_comment':
                  title = `💬 Комментарий в PR #${p.pull_request.number} от ${p.comment.user.login}`;
                  url   = p.comment.html_url;
                  body  = p.comment.body || '';
                  break;
                case 'discussion':
                case 'discussion_comment':
                  title = `💬 Discussion ${p.action}: ${p.discussion.title}`;
                  url   = p.discussion.html_url;
                  body  = (p.comment && p.comment.body) || p.discussion.body || '';
                  break;
                case 'release':
                  title = `🏷️ Release ${p.action}: ${p.release.tag_name}`;
                  url   = p.release.html_url;
                  body  = p.release.body || '';
                  break;
                default:
                  title = `📣 ${event} ${p.action || ''}`.trim();
                  url   = `https://github.com/${owner}/${repo}`;
                  body  = '';
              }
            }

            // Если мы не вышли раньше — собираем сообщение
            const lines = [
              `*${esc(title || 'Событие')}*`,
              '',
              esc(footer),
              body ? '' : null,
              body ? trim(esc(body), 3200) : null,
              '',
              (url || `https://github.com/${owner}/${repo}`)
            ].filter(Boolean);

            core.setOutput('message', lines.join('\n'));
            if (!skip) core.setOutput('skip', 'false');

      - name: Отправить в Telegram
        if: steps.build.outputs.skip != 'true'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          set -euo pipefail
          API_URL="https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage"
          curl -sS "$API_URL" \
            --data-urlencode "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=${{ steps.build.outputs.message }}" \
            --data-urlencode "parse_mode=MarkdownV2" \
            --data-urlencode "disable_web_page_preview=true"
